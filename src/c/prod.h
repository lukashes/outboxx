/* Production C bindings for build-system translate-c: the libraries the shipped
 * binary links. Imported as "c" by the exe and the prod modules. */
#include <libpq-fe.h>
#include <librdkafka/rdkafka.h>
