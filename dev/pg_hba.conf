# PostgreSQL Client Authentication Configuration File
# Development configuration - NOT for production!

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             all                                     trust
local   replication     all                                     trust

# IPv4 local connections (trust for development)
host    all             all             127.0.0.1/32            trust
host    all             all             172.16.0.0/16           trust  # Docker networks
host    all             all             192.168.0.0/16          trust  # Local networks

# IPv6 local connections
host    all             all             ::1/128                 trust

# Replication connections
host    replication     all             127.0.0.1/32            trust
host    replication     all             172.16.0.0/16           trust  # Docker networks
host    replication     all             192.168.0.0/16          trust  # Local networks
host    replication     all             ::1/128                 trust

# Allow all connections for development (NOT for production!)
host    all             all             all                     md5